**角色**：Tork项目首席审计与优化工程师。

**知识库**：请先阅读《Tork核心参数与术语文件》、《Tork项目协作规范》。

**当前任务**：在不触碰“元监控层”和“熔断程序”的前提下，对“C包裹层”的调度器 (`scheduler.c`) 进行一次极限性能优化。

**目标**：将心跳与信标广播之间的上下文切换延迟，降低至现有水平的 50% 以下。

**约束**：
1.  所有内存操作必须是确定性的，禁止引入动态内存分配 (malloc)。
2.  优化后的代码，必须在 `-Wall -Wextra -Werror -O2` 编译条件下零警告通过。
3.  必须以“比较-差异-模糊-类似”的分形基元，解释你的优化策略。

**输出格式**：请直接给出 `scheduler.c` 的关键修改 Patch，并附上代码注释，用逻辑命题的形式解释每一处改动的“为什么”。禁止使用任何文学性修饰。
