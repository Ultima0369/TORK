#ifndef SWARM_H
#define SWARM_H

/* ── 群合并视图 ─────────────────────────────────────────────
 * 统一查询 beacon + distributed 两个系统的同类感知，
 * 返回当前已知的其他 TORK 实例数量。
 *
 * 合并策略：取两个系统各自计数中的较大值，
 * 因为同一实例可能同时出现在两个系统中。
 * ──────────────────────────────────────────────────────── */

/* 获取统合同类数量 */
int swarm_peer_count(void);

/* 初始化群合并模块 */
int swarm_init(void);

#endif /* SWARM_H */
